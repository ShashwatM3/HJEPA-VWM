extends: two_step_rollout_base.yaml

checkpoint_dir: /workspace/ckpt/inv023_two_step_rollout_treatment
train:
  lambda_rollout: 0.10
wandb:
  name: Investigation 23 · Rollout endpoint consistency · Treatment, rollout weight 0.10
