# Next steps — Investigation 004

**While PAUSED:**

1. Complete [investigation_005](investigation_005/) with `lambda_var=0.5`, `lambda_cov=0`.
2. If rank ≥ ~30 and copy ratio holds → **CLOSE** this investigation as unnecessary.
3. If rank stuck ~10–15 with healthy cosine/std → run calibrated `--lambda-cov` A/B; require tech-lead sign-off before locking default.

Do not run VICReg-C before confirming variance-only path on full 15k.
