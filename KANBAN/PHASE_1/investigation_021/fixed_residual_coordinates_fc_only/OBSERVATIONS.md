# Observations — fixed residual coordinates

PLANNED — no W&B run exists yet.

After launch proof, record:

- clean launch commit;
- W&B ID and URL;
- source checkpoint SHA-256;
- `F_c` trainable-initialization hash and all frozen-state hashes;
- step-0 `loss`, `L_flow`, prediction mode, skipped-update state, and process/GPU proof.

After completion, add only a short verdict and links to `METRIC_READOUT.md` and `ANALYSIS.md`.
Do not write metric values until they have been pulled from unsampled W&B history.
