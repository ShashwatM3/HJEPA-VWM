# Paired analysis — Runs 64 and 65

The complete analysis is intentionally interleaved rather than split into two run-by-run reports.
Its canonical copy is
[`../run_064_unwhitened_internal_memory_m512/ANALYSIS.md`](../run_064_unwhitened_internal_memory_m512/ANALYSIS.md),
which compares M=512 and M=1024 at every reading-cycle question and training phase. This pointer
keeps both run folders discoverable without allowing two duplicated reports to drift apart.
