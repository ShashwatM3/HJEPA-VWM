# Observations — Run 66, unwhitened M=512 with covariance plus variance

Run 66 was stopped intentionally at approximately step `10,950` before the 15,000-step endpoint.
The captured run remained numerically healthy, with zero gradient skips, instability warnings,
NaNs, OOMs, or tracebacks. Its W&B history is preserved as a partial regularized reference and
must not be presented as a completed endpoint.
