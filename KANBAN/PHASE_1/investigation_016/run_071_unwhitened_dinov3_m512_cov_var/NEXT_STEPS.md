# Next steps — run 071

1. Reuse W&B `fiactcw6` as the exact tried DINO `32×256` scientific configuration in
   [investigation 017](../../investigation_017/); do not pay for another identical center.
2. Run only the eight untried DINO shapes when that lane resumes.
3. Keep shape conclusions within the DINO lane and qualify the center as recorded-batch collapsed
   despite rank above 60.
4. Require source-diverse diagnostics before a global-collapse claim and a separate run before any
   DINO prediction-readiness claim.
5. Preserve the PR #8 upload inventory alongside the run identity: `7` checkpoint objects
   (`2.9 GiB`) and `1` preflight object in the run-specific `s3://hjepa-volume/` directories.
