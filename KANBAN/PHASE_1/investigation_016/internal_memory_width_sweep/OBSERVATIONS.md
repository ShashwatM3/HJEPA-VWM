# Observations — unwhitened internal-memory width sweep

## Pre-launch

- Two EGO4D arms are preregistered: full internal widths 512 and 1,024.
- Both keep the external abstract code at `32 x 256` and move the only projection to 256 after all
  three latent blocks.
- Whitening and every auxiliary geometry regularizer are off.
- W&B IDs, runtime evidence, and measured outcomes are pending launch.
