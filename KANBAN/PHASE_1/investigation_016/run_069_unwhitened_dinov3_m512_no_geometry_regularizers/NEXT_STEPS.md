# Next steps — run 069

1. Carry the validated pinned DINOv3 adapter into
   [`investigation_017`](../../investigation_017/) with `lambda_var=0.5` and
   `lambda_cov=0.01` enabled for every latent-shape arm.
2. Compare shapes only within the DINO lane; use normalized geometry and conditioned-code
   diagnostics, not raw reconstruction values, for cross-encoder synthesis.
3. Keep source-diverse validation repair open before assigning any global-collapse or global
   preservation claim.
