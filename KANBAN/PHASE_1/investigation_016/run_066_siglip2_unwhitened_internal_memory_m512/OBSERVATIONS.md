# Observations — Run 66, SigLIP 2 unwhitened internal-memory M=512

## Status

PLANNED — not yet launched. No metrics recorded yet.

Fill this file after the run completes:

- W&B ID/URL and final state (`finished`).
- Final checkpoint path + SHA-256.
- Present-only Reading Cycle B table (Q1–Q6).
- Preregistered late-window medians (steps 12,000–14,500): active `L_recon`, correct-code
  `L_recon_present`, shuffled-code `L_recon_shuffled_c`, correct-vs-shuffled gap, exact-chunk
  conditioned share, mean code std, cross-example cosine, effective rank, slot-diversity rank.
- Within-run comparison against the V-JEPA M=512 control (run 064, W&B `4biwq87o`), using
  within-run diagnostics only — **not** raw cross-encoder cosine values.

Do not invent metric values; pull them from W&B.
