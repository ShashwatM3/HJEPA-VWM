# Next steps — Run 64, EGO4D internal memory M=512

1. Keep this completed arm immutable; its W&B run, provenance, and final checkpoint are valid.
2. Use M=512 as the default internal width for the next architecture arm; M=1024 missed both
   preregistered material-effect thresholds and has a 3.86-times larger bottleneck.
3. Treat source-diverse representation health as a separate diagnostic repair, not as a claim this
   fixed within-source batch can answer.
4. Open only one remaining bottleneck axis at a time: external code rate/`D_c`, decoder form, or
   objective honesty. This width pair does not identify which of those is next binding.
5. Restore geometry pressure in a separate experiment if a healthy representation, rather than
   reconstruction-only compression, is the goal.
