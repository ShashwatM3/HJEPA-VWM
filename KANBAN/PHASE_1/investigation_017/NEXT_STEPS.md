# Next steps — investigation_017

1. Treat the prior queue as stopped. Do not describe `ihiuptdp`, its tmux session, or any sweep
   lane as active.
2. Before any relaunch, publish one clean tested commit containing the DINO adapter, `--d-c`
   control, and final sweep artifacts; record its SHA in the launch handoff.
3. Run only the 24 non-center configurations: eight V-JEPA2, eight SigLIP 2, and eight DINOv3.
   Reuse exact scientific centers `guiduvjp`, `ufbeokj2`, and `fiactcw6`. The V-JEPA2 `16×512`
   cell needs a fresh identity because `ihiuptdp` is an incomplete crashed arm.
4. Append W&B IDs/states to the correct bundle as runs stream; do not reuse historical local labels
   or resume an arm from another shape.
5. Analyze the two shape main effects, interaction, and equal-capacity diagonal separately within
   each encoder. Then synthesize normalized geometry, code dependence, stability, and compute
   across encoders.
6. Repeat any marginal lane winner and its center at a second registered seed before changing the
   project default. Close the investigation only after all three lane verdicts are recorded.
