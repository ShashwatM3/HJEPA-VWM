# Next steps — V-JEPA2 latent-shape sweep

1. Treat the old direct queue as stopped. Do not monitor or describe its tmux process as active.
2. If the lane resumes, launch a fresh `16×512` identity plus the seven never-started shapes; the
   reused center is `guiduvjp`, duplicate `kiti1gpc` is excluded, and crashed `ihiuptdp` must not
   be resumed.
3. Record each new W&B ID as soon as its arm streams.
4. Compare completed shapes to the `32×256` center using within-lane loss, normalized geometry,
   correct-versus-shuffled dependence, and compute.
5. Repeat a marginal winner and the center with a second registered seed before changing defaults.
