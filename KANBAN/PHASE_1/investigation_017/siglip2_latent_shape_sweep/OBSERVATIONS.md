# Observations — SigLIP 2 latent-shape sweep

No arm has launched.

Registered prior: reused center Run 67 supplies a stable but only moderately spread equilibrium.
A wider external slot may preserve more SigLIP feature directions, while more slots may mostly
change covariance sampling and decoder memory. Neither is assumed to win; positive rolled-code
separation and geometry must move together.

W&B IDs, states, late-window measurements, and the lane verdict will be appended after launch.
